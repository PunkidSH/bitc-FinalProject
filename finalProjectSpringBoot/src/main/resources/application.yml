server:
  port: 8080

spring:
  application:
    name: finalProjectSpringBoot

  mvc:
    hiddenmethod:
      filter:
        enabled: true

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.100.105.194:3306/testdb1?userSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: test1
    password: full405

  jpa:
    database:
    generate-ddl: true
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
      format_sql: true

  sql:
    init:
      mode: always


  jackson:
    date-format: yyyy-MM-dd HH:mm
    time-zone: UTC

# 필요 시 업로드 file 경로
#file.upload-dir= /~